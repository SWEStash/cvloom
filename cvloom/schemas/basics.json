{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "basics",
  "title": "CV Basics",
  "type": "object",
  "required": ["headline", "summary"],
  "additionalProperties": false,
  "properties": {
    "headline": {
      "type": "string",
      "description": "One-line professional title, e.g. 'Senior Backend Engineer'"
    },
    "summary": {
      "type": "string",
      "description": "2–4 sentence professional summary (Markdown supported)"
    },
    "links": {
      "type": "array",
      "description": "Profile links shown in the CV header. Public by nature, so they live here rather than in private/contact.yaml and render identically in public and private builds.",
      "items": {
        "type": "object",
        "required": ["label", "url"],
        "additionalProperties": false,
        "properties": {
          "label": {"type": "string"},
          "url": {"type": "string", "format": "uri"}
        }
      }
    }
  }
}
